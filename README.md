
---

# 📘 README — Project 2  
## Expense Tracker (Python CLI)

```markdown
# Expense Tracker (Python CLI)

A command-line expense tracking application built in Python that allows users
to record expenses, categorize them, and generate summary reports.

This project strengthens backend fundamentals such as data aggregation,
reporting logic, and modular design.

---

## 🚀 Features

- Add expenses with category, description, and date
- View all recorded expenses
- Generate reports:
  - Total expenses
  - Expenses grouped by category
- Persistent storage using JSON
- Input validation for numeric values

---

## 🧠 Concepts Demonstrated

- Data aggregation and summaries
- Dictionary grouping patterns
- File handling with JSON
- Modular design (`storage`, `reports`, `main`)
- Defensive coding against invalid input
- Use of standard library modules (`datetime`)

---

## 📁 Project Structure

